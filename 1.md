flowchart TB

    %% =========================
    %% Access Layer
    %% =========================

    User[Пользователь]

    Keycloak[Keycloak<br/>SSO / AuthN / AuthZ<br/>Роли и группы]

    User --> Keycloak

    %% =========================
    %% Applications
    %% =========================

    subgraph Applications["Корпоративные сервисы"]
        Portal[Веб-портал]
        Nextcloud[Nextcloud]
        FutureApps[Будущие приложения<br/>Mobile / Desktop / API]
    end

    Keycloak --> Portal
    Keycloak --> Nextcloud
    Keycloak --> FutureApps

    %% =========================
    %% AI Layer
    %% =========================

    Portal --> AIAgent

    AIAgent[AI Chat Agent<br/>Function Calling]

    AIAgent --> PolicyEngine

    PolicyEngine[Проверка прав доступа<br/>по ролям и группам<br/>из Keycloak]

    PolicyEngine <-->|OIDC / Claims| Keycloak

    %% =========================
    %% Security Domains
    %% =========================

    subgraph L1["Уровень 1 - Публичная информация"]
        DB1[(Public DB)]
        Q1[(Qdrant Public)]
        M1[LLM Public]
    end

    subgraph L2["Уровень 2 - Коммерческая информация"]
        DB2[(Commercial DB)]
        Q2[(Qdrant Commercial)]
        M2[LLM Commercial]
    end

    subgraph L3["Уровень 3 - R&D"]
        DB3[(R&D DB)]
        Q3[(Qdrant R&D)]
        M3[LLM R&D]
    end

    %% =========================
    %% Access Control
    %% =========================

    PolicyEngine -->|Level 1 Access| DB1
    PolicyEngine -->|Level 1 Access| Q1
    PolicyEngine -->|Level 1 Access| M1

    PolicyEngine -->|Level 2 Access| DB2
    PolicyEngine -->|Level 2 Access| Q2
    PolicyEngine -->|Level 2 Access| M2

    PolicyEngine -->|Level 3 Access| DB3
    PolicyEngine -->|Level 3 Access| Q3
    PolicyEngine -->|Level 3 Access| M3

    %% =========================
    %% RAG Flows
    %% =========================

    M1 <--> Q1
    M2 <--> Q2
    M3 <--> Q3

    %% =========================
    %% Data Sources
    %% =========================

    Nextcloud --> DB1
    Nextcloud --> DB2
    Nextcloud --> DB3

    Portal --> DB1
    Portal --> DB2
    Portal --> DB3

    %% =========================
    %% Isolation
    %% =========================

    classDef public fill:#d5f5d5
    classDef commercial fill:#fff2cc
    classDef rnd fill:#f4cccc

    class DB1,Q1,M1 public
    class DB2,Q2,M2 commercial
    class DB3,Q3,M3 rnd